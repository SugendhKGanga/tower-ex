---
- name: Hello World!
  hosts: all
  tasks:
  - name: Hello World!
    shell: echo "Hi! Tower is working."
  - name: Install helm chart
    become: yes
    become_user: root
    become_method: su
    shell: echo "chart name is {{ chart_name }}" > /tmp/test.txt
